

# ТЗ: Улучшение 5 режимов AI (иконки над чатом)

## Текущее состояние

На скриншоте видно 5 иконок-режимов:
1. **Обсудить** (MessageSquare) — свободный диалог
2. **Анализ** (BarChart3) — выявление закономерностей  
3. **Черновик** (FileEdit) — создание текстов
4. **Расчёт** (Calculator) — вычисления
5. **План** (ListTodo) — пошаговые планы

### Проблемы

| Режим | Текущий статус | Проблема |
|-------|---------------|----------|
| Обсудить | Работает | Базовый режим, но нет специфичного UI |
| Анализ | Частично | Нет визуализации результатов анализа |
| Черновик | Работает | DraftArtifact карточка отображается |
| Расчёт | Частично | Нет форматирования формул/таблиц |
| План | Частично | Нет интерактивных чекбоксов |

Все режимы передают промпт в AI, но UI не адаптируется к типу ответа.

---

## Предлагаемые улучшения

### 1. Режим "Анализ" — Структурированный вывод

**Что добавить:**
- AI возвращает `analysisArtifact` с секциями
- UI отображает карточку анализа с разделами:
  - Ключевые паттерны
  - Риски/проблемы  
  - Выводы/рекомендации

```typescript
// Новый тип артефакта
interface AnalysisArtifact {
  type: 'analysis';
  summary: string;
  patterns?: string[];      // Закономерности
  risks?: string[];         // Риски
  conclusions?: string[];   // Выводы
}
```

**Файлы:**
- `src/lib/ai/discussions.ts` — добавить в промпт
- `src/components/discussions/AnalysisArtifact.tsx` — новый компонент
- `src/pages/DiscussionChatPage.tsx` — рендер артефакта

### 2. Режим "Расчёт" — Форматирование вычислений

**Что добавить:**
- AI возвращает `computeArtifact` с шагами
- UI отображает калькулятор-карточку:
  - Входные данные (inputs)
  - Шаги расчёта (steps)
  - Итоговый результат (result)

```typescript
interface ComputeArtifact {
  type: 'compute';
  inputs: { label: string; value: string }[];
  steps: string[];          // Шаги расчёта
  result: string;           // Итоговое значение
  assumptions?: string[];   // Допущения
}
```

**Файлы:**
- `src/lib/ai/discussions.ts` — обновить промпт
- `src/components/discussions/ComputeArtifact.tsx` — новый компонент

### 3. Режим "План" — Интерактивный чеклист

**Что добавить:**
- AI возвращает `planArtifact` со списком задач
- UI отображает чеклист:
  - Пункты с чекбоксами
  - Возможность отмечать выполненные
  - Копирование плана в буфер

```typescript
interface PlanArtifact {
  type: 'plan';
  title: string;
  items: { 
    text: string; 
    priority?: 'high' | 'medium' | 'low';
    dueHint?: string;  // "сегодня", "завтра", "на неделе"
  }[];
}
```

**Файлы:**
- `src/lib/ai/discussions.ts` — обновить промпт
- `src/components/discussions/PlanArtifact.tsx` — новый компонент

### 4. Улучшить "Обсудить" — Follow-up вопросы

**Что добавить:**
- AI возвращает `questions: string[]` (уже есть в типах)
- UI показывает кнопки-чипы с вопросами для продолжения

```typescript
// Уже есть в DiscussionAIResponse:
questions?: string[];  // Follow-up вопросы
```

**Файлы:**
- `src/lib/ai/discussions.ts` — добавить инструкцию генерировать вопросы
- `src/pages/DiscussionChatPage.tsx` — отображать кнопки вопросов

### 5. Подсказки-плейсхолдеры для каждого режима

**Что добавить:**
- Динамический placeholder в поле ввода
- Показывает примеры запросов для активного режима

```typescript
const MODE_PLACEHOLDERS: Record<DiscussionMode, { ru: string; en: string }> = {
  discuss: { 
    ru: 'Что ты думаешь о...', 
    en: 'What do you think about...' 
  },
  analyze: { 
    ru: 'Проанализируй закономерности в...', 
    en: 'Analyze patterns in...' 
  },
  draft: { 
    ru: 'Напиши письмо о...', 
    en: 'Write an email about...' 
  },
  compute: { 
    ru: 'Посчитай сколько...', 
    en: 'Calculate how much...' 
  },
  plan: { 
    ru: 'Составь план для...', 
    en: 'Create a plan for...' 
  },
};
```

---

## Обновлённые промпты для AI

### Промпт для режима "Анализ"

```
Режим АНАЛИЗ: структурируй информацию, выяви закономерности, причины, риски.

ФОРМАТ ОТВЕТА для анализа:
{
  "answer": "Краткое пояснение анализа...",
  "usedEvidenceIds": ["E1", "B1"],
  "analysisArtifact": {
    "type": "analysis",
    "summary": "Общий вывод",
    "patterns": ["Паттерн 1", "Паттерн 2"],
    "risks": ["Риск 1"],
    "conclusions": ["Рекомендация 1", "Рекомендация 2"]
  }
}
```

### Промпт для режима "Расчёт"

```
Режим РАСЧЁТ: покажи шаги вычислений и допущения.

ФОРМАТ ОТВЕТА для расчёта:
{
  "answer": "Объяснение расчёта...",
  "usedEvidenceIds": ["E1"],
  "computeArtifact": {
    "type": "compute",
    "inputs": [{"label": "Цена", "value": "1500 ₽"}],
    "steps": ["Шаг 1: ...", "Шаг 2: ..."],
    "result": "4500 ₽",
    "assumptions": ["Курс доллара: 90 ₽"]
  }
}
```

### Промпт для режима "План"

```
Режим ПЛАН: создай пошаговый план с чеклистом.

ФОРМАТ ОТВЕТА для плана:
{
  "answer": "Вот план действий...",
  "usedEvidenceIds": ["E1"],
  "planArtifact": {
    "type": "plan", 
    "title": "Название плана",
    "items": [
      {"text": "Задача 1", "priority": "high", "dueHint": "сегодня"},
      {"text": "Задача 2", "priority": "medium"},
      {"text": "Задача 3", "priority": "low"}
    ]
  }
}
```

---

## Структура файлов

```
src/components/discussions/
├── ModeSelector.tsx         (существует)
├── DraftArtifact.tsx        (существует)
├── AnalysisArtifact.tsx     (СОЗДАТЬ)
├── ComputeArtifact.tsx      (СОЗДАТЬ)
├── PlanArtifact.tsx         (СОЗДАТЬ)
├── FollowUpQuestions.tsx    (СОЗДАТЬ)
└── EvidenceCard.tsx         (существует)
```

---

## Изменения в DiscussionAIResponse

```typescript
export interface DiscussionAIResponse {
  answer: string;
  usedEvidenceIds: string[];
  
  // Артефакты по режимам
  draftArtifact?: DraftArtifact;      // Существует
  analysisArtifact?: AnalysisArtifact; // НОВОЕ
  computeArtifact?: ComputeArtifact;   // НОВОЕ  
  planArtifact?: PlanArtifact;         // НОВОЕ
  
  questions?: string[];  // Follow-up вопросы (существует, но не используется)
}
```

---

## UI обновления в MessageBubble

```tsx
// После Draft Artifact добавить:
{message.meta?.analysisArtifact && (
  <AnalysisArtifact artifact={message.meta.analysisArtifact} />
)}

{message.meta?.computeArtifact && (
  <ComputeArtifact artifact={message.meta.computeArtifact} />
)}

{message.meta?.planArtifact && (
  <PlanArtifact artifact={message.meta.planArtifact} />
)}

{message.meta?.questions && message.meta.questions.length > 0 && (
  <FollowUpQuestions 
    questions={message.meta.questions} 
    onSelect={setInputText}
  />
)}
```

---

## Приоритет реализации

| Этап | Компонент | Сложность | Польза |
|------|-----------|-----------|--------|
| 1 | PlanArtifact + чеклист | Средняя | Высокая |
| 2 | FollowUpQuestions | Низкая | Средняя |
| 3 | Динамические плейсхолдеры | Низкая | Средняя |
| 4 | AnalysisArtifact | Средняя | Средняя |
| 5 | ComputeArtifact | Средняя | Низкая |

---

## Ожидаемый результат

После реализации каждый режим будет:
1. Показывать специфичный плейсхолдер в поле ввода
2. Возвращать структурированный артефакт
3. Отображать артефакт в специальной карточке
4. Предлагать follow-up вопросы для продолжения диалога

```
+--------------------------------------------------+
|  [Обсудить] [Анализ] [Черновик] [Расчёт] [План]  |
+--------------------------------------------------+
|  Пользователь: Составь план на завтра            |
+--------------------------------------------------+
|  Ассистент: Вот план на завтра...                |
|                                                  |
|  +----------------------------------------------+|
|  | 📋 ПЛАН: Завтрашний день                     ||
|  +----------------------------------------------+|
|  | ☐ Утро: Встреча с командой  [⚡ срочно]       ||
|  | ☐ Обед: Звонок клиенту                       ||
|  | ☐ Вечер: Отправить отчёт                     ||
|  | [📋 Скопировать] [➕ Добавить в напоминания] ||
|  +----------------------------------------------+|
|                                                  |
|  Вопросы: [Добавить детали?] [Изменить время?]  |
+--------------------------------------------------+
```

