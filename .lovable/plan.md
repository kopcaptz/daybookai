
# ТЗ: Исправление выбора даты в календаре напоминаний

## Проблема

На скриншоте видно, что календарь открывается, но **даты не кликаются**. Это происходит из-за конфликта z-index:

| Компонент | z-index | Результат |
|-----------|---------|-----------|
| Sheet (底部 панель) | z-50 | Перекрывает всё |
| Sheet Overlay (затемнение) | z-50 | Блокирует клики |
| PopoverContent (календарь) | z-50 | Конкурирует с Sheet |

Когда Popover использует `Portal`, его контент рендерится **вне** Sheet, но под его overlay — и клики перехватываются overlay.

---

## Решение

### Вариант А: Увеличить z-index для PopoverContent внутри Sheet

Добавить более высокий z-index для PopoverContent в `QuickReminderSheet`:

```typescript
<PopoverContent 
  className="w-auto p-0 z-[60]" 
  align="start"
>
```

### Вариант Б: Использовать `modal={false}` для Popover

Отключить модальное поведение Popover, чтобы он не блокировался Sheet overlay:

```typescript
<Popover 
  open={showDatePicker} 
  onOpenChange={setShowDatePicker}
  modal={false}
>
```

### Вариант В (рекомендуемый): Комбинация

Применить оба исправления для надёжности:

1. `modal={false}` — позволяет Popover работать внутри другого модального окна
2. `z-[60]` — гарантирует, что календарь выше Sheet overlay

---

## Дополнительные улучшения UX

### 1. Закрывать Popover после выбора даты

Сейчас после выбора даты Popover остаётся открытым. Логичнее закрывать:

```typescript
const handleCustomDateSelect = (date: Date | undefined) => {
  setCustomDate(date);
  if (date) {
    setSelectedChip(null);
    setShowDatePicker(false); // <-- Закрыть календарь
  }
};
```

### 2. Добавить кнопку очистки выбранной даты

Если пользователь выбрал кастомную дату, но хочет вернуться к чипам — добавить кнопку "×":

```text
┌────────────────────────────┬─────────┬───┐
│  📅 31.01.2026            │  09:00  │ × │
└────────────────────────────┴─────────┴───┘
```

### 3. Показывать выбранный день чипа

После выбора чипа показывать конкретную дату:

```text
Завтра утром → "Завтра утром (1 фев, 09:00)"
```

---

## Технические изменения

### Файл: `src/components/reminders/QuickReminderSheet.tsx`

1. Добавить `modal={false}` к Popover
2. Добавить `z-[60]` к PopoverContent
3. Закрывать Popover после выбора даты
4. Добавить кнопку очистки кастомной даты
5. Показывать время для выбранного чипа

### Опционально: `src/components/ui/popover.tsx`

Можно глобально повысить z-index PopoverContent до `z-[60]`, но это может повлиять на другие места. Лучше применить локально.

---

## Итоговый UI

```text
┌─────────────────────────────────────────────────┐
│  🔔 Новое напоминание                           │
├─────────────────────────────────────────────────┤
│  Что напомнить?                                 │
│  ┌──────────────────────────────────────────┐   │
│  │ Позвонить в банк                         │   │
│  └──────────────────────────────────────────┘   │
│                                                 │
│  Когда?                                         │
│  [Позже сегодня] [Завтра утром] [Выходные]      │
│                                                 │
│  ┌──────────────────────┬─────────┬───┐         │
│  │  📅 01.02.2026      │  09:00  │ × │         │
│  └──────────────────────┴─────────┴───┘         │
│                                                 │
│  Повторять                                      │
│  [Нет] [Ежедневно] [Еженедельно] [Ежемесячно]   │
│                                                 │
│       [Отмена]           [Создать]              │
└─────────────────────────────────────────────────┘
```

---

## Проверки после реализации

1. Открыть QuickReminderSheet
2. Нажать "Выбрать время..."
3. Убедиться, что календарь кликабелен
4. Выбрать дату → календарь закрывается
5. Появляется поле времени
6. Нажать "×" → кастомная дата сбрасывается, можно выбрать чип
