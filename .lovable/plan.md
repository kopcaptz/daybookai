
# ТЗ: Замена иконки чата на ScanEye

## Цель
Заменить иконку `MessageCircle` на `ScanEye` в плавающей кнопке чата, чтобы лучше отразить функцию "скрин в чат".

## Файл для изменения
**Только один файл:** `src/components/FloatingChatButton.tsx`

## Изменения

### 1. Импорт иконки
**Строка 2** — добавить `ScanEye` в импорт:
```typescript
// Было:
import { MessageCircle, X, Camera } from 'lucide-react';

// Станет:
import { MessageCircle, X, ScanEye } from 'lucide-react';
```
> `Camera` можно удалить — она не используется

### 2. Иконка в FAB-кнопке
**Строка 128** — заменить иконку в плавающей кнопке:
```typescript
// Было:
<MessageCircle className="h-6 w-6 text-primary-foreground relative z-10" strokeWidth={2} />

// Станет:
<ScanEye className="h-6 w-6 text-primary-foreground relative z-10" strokeWidth={2} />
```

### 3. Иконка в заголовке Sheet
**Строка 155** — заменить иконку в шапке открытого чата:
```typescript
// Было:
<MessageCircle className="h-4 w-4 text-primary-foreground" />

// Станет:
<ScanEye className="h-4 w-4 text-primary-foreground" />
```

## Что НЕ трогаем
- Логику работы кнопки
- Анимации и стили
- Обработчики событий
- Iframe и auto-screenshot логику
- Любые другие файлы

## Результат
| Место | Было | Станет |
|-------|------|--------|
| FAB-кнопка | 💬 MessageCircle | 👁 ScanEye |
| Заголовок Sheet | 💬 MessageCircle | 👁 ScanEye |

## Визуально
```
   Было:                    Станет:
   
   [💬]  ← FAB кнопка       [👁]  ← FAB кнопка
   
   ┌─────────────────┐      ┌─────────────────┐
   │ 💬 Чат          │      │ 👁 Чат          │
   │    AI-ассистент │  →   │    AI-ассистент │
   └─────────────────┘      └─────────────────┘
```

## Итого: 3 строки кода
1. Импорт: +ScanEye, -Camera
2. Строка 128: MessageCircle → ScanEye
3. Строка 155: MessageCircle → ScanEye
